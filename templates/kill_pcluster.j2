#!/bin/bash
set -euo pipefail
# Generated by ParallelClusterMaker
# Teardown wrapper for {{ cluster_name }}
echo "Deleting cluster {{ cluster_name }}..."
pcluster delete-cluster --cluster-name "{{ cluster_name }}" --region "{{ region }}"
