aws-parallelcluster==3.15.1
boto3
botocore
# Not for running playbooks -- there are none. `pcluster_core._template_env`
# renders every template with ansible.builtin.template's own trim_blocks/
# lstrip_blocks defaults, read out of this package by
# TestTheTestEnvironmentMatchesAnsible, and make_pcluster.py aborts a build
# when `ansible --version` does not run.
ansible
Jinja2
PyYAML
ruamel.yaml
pytest
# Undefined-name gate (tests/test_undefined_names.py). Nothing else in the
# toolchain checks Python for undefined names, and a NameError inside a
# broad `except Exception` reads as a benign runtime warning.
pyflakes
# Named directly rather than relied on as a transitive of botocore.
jmespath
# MCP server (Workstream 5). The standalone `fastmcp` package, deliberately
# not the official `mcp` SDK's raw low-level API -- the SDK vendors an older
# copy of the same decorator API internally, and nothing in this tool surface
# needs the SDK's low-level protocol control. `fastmcp` depends on `mcp`
# anyway, so the SDK arrives transitively. pytest-asyncio is required by the
# in-memory FastMCP `Client` tests.
fastmcp
pytest-asyncio
# Workstream 6: the Lambda authorizer verifies Cognito's RS256 access
# tokens against its JWKS. PyJWT arrives transitively via `mcp`, but the
# authorizer's Lambda artifact carries neither `mcp` nor `fastmcp` -- so
# it is named directly rather than relied on as a transitive.
PyJWT
cryptography

# hpc-benchmark/ script dependencies
numpy
scipy
pandas
matplotlib
seaborn

# Linting. `make lint` runs `ruff check` (never --fix -- see the Makefile).
ruff
